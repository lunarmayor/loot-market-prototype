import styled from "@emotion/styled";
import { space } from "styled-system";

const H1 = styled.h1`
  margin: 0;
  font-size: 40px;
  color: white;
  font-family: Serif;
  ${space}
`;

export default H1;
