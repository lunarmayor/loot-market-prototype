import styled from "@emotion/styled";
import { space } from "styled-system";

const H2 = styled.h2`
  color: #29272a;
  font-family: Source Code Pro;
  margin: 0;
  font-size: 16px;
  ${space}
`;

export default H2;
