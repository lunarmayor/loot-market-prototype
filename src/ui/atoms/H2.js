import styled from "@emotion/styled";
import { space } from "styled-system";

const H2 = styled.h1`
  margin: 0;
  font-size: 16px;
  font-family: SupercellText Bold;
  ${space}
`;

export default H2;
